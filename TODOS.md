# Global Recall — TODOs

Generated by /plan-eng-review · 2026-05-12 · Branch: main
Updated by session · 2026-05-12

---

## Phase 1 — Onboarding (Activation)

- [x] **P1 · Portrait layout** — Removed `"orientation": "landscape-primary"` from manifest.json (changed to `"any"`). Added `@media (orientation: portrait)` CSS block in index.html to compact header, input, and chip-strip for portrait viewports.

- [x] **P1 · HUD loop throttle** — Replaced `updateHUD()` call in the `requestAnimationFrame` tick with a throttled timer-only update (~1fps). Score and remaining-badge now update only on match (via `updateHUD()` in the input handler). Eliminates 60fps DOM writes.

- [x] **P1 · Auto-rotate pause** — Added `focus`/`blur` event listeners on `#cinput`. Globe stops spinning when user focuses the input field, resumes on blur. Saves GPU while typing.

---

## Migration PR (ES Modules refactor)

- [x] **Vitest + countries.test.js** — Added `package.json`, `src/core.js` (extracted `norm`, `lev`, `findMatch`, `fmt`, `buildCountries`), and `src/core.test.js` (23 tests, all passing). Run with `npm test`.

- [x] **Ghost replay TODO comment** — Added `// TODO: ghost replay hooks in here` at `ghostData = getGhost()` in `startGame()`.

- [x] **SW CACHE CHECKLIST comment** — Added comment block in `sw.js` above PRECACHE explaining the version-bump requirement.

---

## Phase 2 — Share Card + Analytics

- [x] **Canvas CORS verification** — Confirmed: jsDelivr returns `Access-Control-Allow-Origin: *` for both the Three-globe textures and the Natural Earth GeoJSON. `canvas.toDataURL()` will not throw SecurityError. Share card design can proceed.

---

## Phase 2 or dedicated PR

- [ ] **Alias dictionary expansion** — Expanded significantly from ~10–15 to ~30–50 aliases/country in this session (official names, historical names, common abbreviations). Priority entries (Myanmar=Burma, Czechia, Ivory Coast, Eswatini, North Macedonia) were already covered. Further expansion toward 100 aliases/country remains a Phase 3 polish item. (`index.html` `RAW` array)

---

## Performance / PWA

- [x] **SW texture pre-cache trim** — Removed `earth-night.jpg` and `earth-dark.jpg` from sw.js PRECACHE. Bumped CACHE to `global-recall-v3`. Night/dark textures are lazy-cached on first style switch via the fetch handler. Saves ~4MB on first install.
